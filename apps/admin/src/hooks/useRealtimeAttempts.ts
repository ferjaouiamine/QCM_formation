import{useEffect}from'react';import{useQueryClient}from'@tanstack/react-query';import{supabase}from'../lib/supabase';export function useRealtimeAttempts(){const client=useQueryClient();useEffect(()=>{const channel=supabase.channel('admin-attempts').on('postgres_changes',{event:'*',schema:'public',table:'attempts'},()=>client.invalidateQueries()).subscribe();return()=>{void supabase.removeChannel(channel)}},[client])}
