import{FormEvent,useState}from'react';import{supabase}from'../lib/supabase';export default function Login(){const[email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''),[busy,setBusy]=useState(false);const submit=async(e:FormEvent)=>{e.preventDefault();setBusy(true);setError('');const{error:x}=await supabase.auth.signInWithPassword({email,password});if(x)setError('Identifiants incorrects ou compte non autorisé.');setBusy(false)};return <main className="login-page"><section className="login-panel"><div className="login-brand"><span className="brand-mark">FS</span><div><strong>Finasure</strong><small>Portail de formation</small></div></div><div className="login-copy"><p className="eyebrow">Espace sécurisé</p><h1>Dashboard administrateur</h1><p>Suivez les évaluations, identifiez les axes de progression et pilotez la performance du réseau.</p></div></section><section className="login-form-wrap"><form className="login-card" onSubmit={submit}><div className="mobile-logo"><span className="brand-mark">FS</span></div><p className="eyebrow">Bienvenue</p><h2>Connexion</h2><p className="muted">Accédez aux résultats de formation.</p>{error&&<p className="alert" role="alert">{error}</p>}<label>Adresse e-mail<input type="email" autoComplete="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="admin@finasure.tn"/></label><label>Mot de passe<input type="password" autoComplete="current-password" required value={password} onChange={e=>setPassword(e.target.value)} placeholder="••••••••"/></label><button className="button primary full" disabled={busy}>{busy?'Connexion…':'Se connecter'}</button><p className="secure-note">Accès réservé aux administrateurs autorisés</p></form></section></main>}
