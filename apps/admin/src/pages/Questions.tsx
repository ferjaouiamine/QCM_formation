import{useQuery}from'@tanstack/react-query';import{supabase}from'../lib/supabase';const fmt=(v:unknown)=>Number(v??0).toLocaleString('fr-FR',{maximumFractionDigits:1});export default function Questions(){const{data,isLoading,error}=useQuery({queryKey:['question-stats'],queryFn:async()=>{const{data,error}=await supabase.from('admin_question_stats').select('*').order('success_rate');if(error)throw error;return data}});return <><header className="page-header"><div><p className="eyebrow">Analyse pédagogique</p><h1>Performance par question</h1><p>Repérez les notions difficiles et les questions fréquemment ignorées.</p></div></header><section className="card">{isLoading?<div className="skeleton table-block"/>:error?<p className="alert">Erreur de chargement.</p>:!data?.length?<div className="empty-state"><h2>Aucune donnée disponible</h2></div>:<div className="question-list">{data.map(q=><article className="question-stat" key={q.id}><span className="question-number">{q.id}</span><div className="question-main"><small>{q.section}</small><h2>{q.text}</h2><div className="success-line"><div className="progress"><i style={{width:`${Number(q.success_rate??0)}%`}}/></div><strong>{fmt(q.success_rate)} % de réussite</strong></div></div><dl><div><dt>Réponses</dt><dd>{q.responses??0}</dd></div><div><dt>Partielles</dt><dd>{fmt(q.partial_rate)} %</dd></div><div><dt>Sans réponse</dt><dd>{fmt(q.unanswered_rate)} %</dd></div></dl></article>)}</div>}</section></>}
