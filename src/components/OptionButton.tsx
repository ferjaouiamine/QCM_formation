import type {Option,OptionId} from '../types/quiz';
export default function OptionButton({option,type,checked,onChange}:{option:Option;type:'single'|'multiple';checked:boolean;onChange:(id:OptionId)=>void}){return <label className={`quiz-option flex min-h-11 cursor-pointer items-center rounded-lg border transition ${checked?'border-accent bg-cyan-50':'border-slate-300 bg-white hover:border-slate-400'}`}><input type={type==='single'?'radio':'checkbox'} checked={checked} onChange={()=>onChange(option.id)} className="quiz-option-input shrink-0 accent-[#176b87]"/><span className="quiz-option-label"><b className="quiz-option-letter uppercase">{option.id}.</b>{option.label}</span></label>}