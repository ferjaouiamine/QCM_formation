import type {Option,OptionId} from '../types/quiz';
export default function OptionButton({option,type,checked,onChange}:{option:Option;type:'single'|'multiple';checked:boolean;onChange:(id:OptionId)=>void}){return <label className={`flex min-h-14 cursor-pointer items-center gap-3 rounded-lg border p-4 transition ${checked?'border-accent bg-cyan-50':'border-slate-300 bg-white hover:border-slate-400'}`}><input type={type==='single'?'radio':'checkbox'} checked={checked} onChange={()=>onChange(option.id)} className="h-5 w-5 accent-[#176b87]"/><span><b className="mr-2 uppercase">{option.id}.</b>{option.label}</span></label>}
