import type {OptionId,Question} from '../types/quiz';
import OptionButton from './OptionButton';
export default function QuestionCard({question,selected,onSelect}:{question:Question;selected:OptionId[];onSelect:(id:OptionId)=>void}){return <section aria-labelledby="question-title" role={question.type==='single'?'radiogroup':'group'} className="quiz-card rounded-lg bg-white shadow-sm">{question.type==='multiple'&&<span className="quiz-multiple-badge inline-block rounded bg-cyan-100 font-semibold text-cyan-900">Plusieurs réponses attendues</span>}<h1 id="question-title" className="quiz-question-title font-bold text-navy">{question.text}</h1><div className="quiz-options">{question.options.map(option=><OptionButton key={option.id} option={option} type={question.type} checked={selected.includes(option.id)} onChange={onSelect}/>)}</div></section>}