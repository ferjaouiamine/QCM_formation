import {formatScore,interpretation} from '../lib/scoring';
export default function ResultSummary({score,max,passing}:{score:number;max:number;passing:number}){const i=interpretation(score),ok=score>=passing;const colors={emerald:'border-emerald-500 bg-emerald-50',lime:'border-lime-600 bg-lime-50',amber:'border-amber-500 bg-amber-50',red:'border-red-500 bg-red-50'};return <section className={`print-card rounded-lg border-l-4 p-6 shadow-sm ${colors[i.color]}`}><div className="flex flex-wrap items-center justify-between gap-4"><div><p className="text-4xl font-bold text-navy">{formatScore(score)} / {max}</p><p className="mt-1 text-lg">{formatScore(score/max*100)} %</p></div><span className={`rounded px-4 py-2 font-bold ${ok?'bg-emerald-700':'bg-red-700'} text-white`}>{ok?'RÉUSSI':'NON VALIDÉ'}</span></div><h2 className="mt-5 text-xl font-bold">{i.level}</h2><p className="mt-2">{i.next}</p></section>}
