import type {CorrectionAnswer} from '../types/quiz';import {formatScore} from '../lib/scoring';
export default function SectionBreakdown({results}:{results:CorrectionAnswer[]}){return <section className="print-card mt-6 rounded-lg bg-white p-5 shadow-sm"><h2 className="mb-4 text-xl font-bold text-navy">Répartition par partie</h2><div className="space-y-4">{[...new Set(results.map(r=>r.section))].map(section=>{const rows=results.filter(r=>r.section===section),score=rows.reduce((n,r)=>n+r.points,0);return <div key={section}><div className="mb-1 flex justify-between text-sm"><b>{section}</b><span>{formatScore(score)} / {rows.length}</span></div><div className="h-3 rounded bg-slate-200"><div className="h-full rounded bg-accent" style={{width:`${score/rows.length*100}%`}}/></div></div>})}</div></section>}
