import {FormEvent,useState} from 'react';import type {Candidate} from '../types/quiz';import {QUIZ_META as QUIZ} from '../data/questions';
export default function StartScreen({onStart,resume,onResume}:{onStart:(c:Candidate)=>void;resume:boolean;onResume:()=>void}){const[name,setName]=useState(''),[agency,setAgency]=useState('');const submit=(e:FormEvent)=>{e.preventDefault();if(name.trim()&&agency.trim())onStart({name:name.trim(),agency:agency.trim()})};return <main className="mx-auto max-w-3xl px-4 py-10"><header className="mb-8"><p className="font-semibold text-accent">{QUIZ.subtitle}</p><h1 className="mt-2 text-3xl font-bold text-navy">{QUIZ.title}</h1></header><section className="mb-6 rounded-lg border bg-white p-5 shadow-sm"><h2 className="font-bold text-navy">Consignes</h2><p className="mt-2 leading-7">{QUIZ.instructions}</p><p className="mt-4 font-semibold">20 questions · 20 points · 30 minutes · seuil 14/20</p></section>{resume&&<button className="mb-6 min-h-11 w-full rounded bg-accent px-5 py-3 font-semibold text-white" onClick={onResume}>Reprendre la tentative en cours</button>}<form onSubmit={submit} className="space-y-4 rounded-lg bg-white p-5 shadow-sm"><label className="block font-semibold">Nom et prénom<input required value={name} onChange={e=>setName(e.target.value)} className="mt-1 min-h-11 w-full rounded border px-3 font-normal"/></label><label className="block font-semibold">Agence / Direction<input required value={agency} onChange={e=>setAgency(e.target.value)} className="mt-1 min-h-11 w-full rounded border px-3 font-normal"/></label><button className="min-h-11 w-full rounded bg-navy px-5 py-3 font-semibold text-white">Commencer l’évaluation</button></form></main>}
