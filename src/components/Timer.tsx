import {useEffect,useState} from 'react';
export default function Timer({expiresAt,onExpire}:{expiresAt:string;onExpire:()=>void}){const calc=()=>Math.max(0,Math.ceil((new Date(expiresAt).getTime()-Date.now())/1000));const[left,setLeft]=useState(calc);useEffect(()=>{const id=setInterval(()=>{const n=calc();setLeft(n);if(n===0){clearInterval(id);onExpire()}},1000);return()=>clearInterval(id)},[expiresAt,onExpire]);return <span role="timer" className={`font-mono font-bold ${left<300?'text-red-600':'text-navy'}`}>{String(Math.floor(left/60)).padStart(2,'0')}:{String(left%60).padStart(2,'0')}</span>}
